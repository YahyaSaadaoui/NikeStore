name: Repository Health Check

on:
  schedule:
    - cron: "29 5 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  health:
    uses: YahyaSaadaoui/YahyaSaadaoui/.github/workflows/reusable-repository-health.yml@main
